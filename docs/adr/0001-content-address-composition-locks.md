# Content-address Composition Locks

Manifest Versions identify immutable declarations but may contain mutable selectors, so they do not identify an executable Goal System. Resolution persists a schema-versioned Composition Lock, canonicalized with RFC 8785 and identified as `sha256:<hex>`; this makes identical resolutions deterministic, exposes component drift, and keeps run identity independent from publication and alias movement.
