{
  "name": "goal-system-composition",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "example:seed": "bun --env-file=.env poc/dependency-upgrade-instrumented/src/seed-prompt.ts",
    "example": "bun --env-file=.env poc/dependency-upgrade-instrumented/src/cli.ts",
    "example:live": "bun --env-file=.env poc/dependency-upgrade-instrumented/src/cli.ts --live",
    "example:live:model": "bun --env-file=.env poc/dependency-upgrade-instrumented/src/cli.ts --live --allow-external-model"
  },
  "dependencies": {
    "@arizeai/phoenix-client": "6.12.0",
    "@arizeai/phoenix-otel": "1.0.2",
    "@earendil-works/pi-coding-agent": "0.80.2",
    "@effect/opentelemetry": "0.63.0",
    "@opentelemetry/api": "1.9.0",
    "canonicalize": "3.0.0",
    "effect": "3.21.4",
    "typebox": "1.0.63"
  },
  "devDependencies": {
    "@types/node": "24.0.15",
    "typescript": "5.8.3"
  },
  "engines": {
    "bun": ">=1.2.17"
  }
}
