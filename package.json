{
  "name": "goal-system-composition",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "example:seed": "node --env-file-if-exists=.env --import tsx poc/dependency-upgrade-instrumented/src/seed-prompt.ts",
    "example": "node --env-file-if-exists=.env --import tsx poc/dependency-upgrade-instrumented/src/cli.ts",
    "example:live": "node --env-file-if-exists=.env --import tsx poc/dependency-upgrade-instrumented/src/cli.ts --live",
    "example:live:model": "node --env-file-if-exists=.env --import tsx poc/dependency-upgrade-instrumented/src/cli.ts --live --allow-external-model"
  },
  "dependencies": {
    "@arizeai/phoenix-client": "6.12.0",
    "@arizeai/phoenix-otel": "1.0.2",
    "@earendil-works/pi-coding-agent": "0.80.2",
    "@effect/opentelemetry": "0.63.0",
    "@opentelemetry/api": "1.9.0",
    "canonicalize": "3.0.0",
    "effect": "3.21.4",
    "typebox": "1.0.63"
  },
  "devDependencies": {
    "@types/node": "24.0.15",
    "tsx": "4.20.3",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
